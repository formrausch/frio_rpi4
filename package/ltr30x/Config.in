config BR2_PACKAGE_LTR30X
	bool "ltr30x"
	help
	  ltr30x

          https://github.com/Seeed-Studio/seeed-linux-dtoverlays

comment "ltr30x needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
